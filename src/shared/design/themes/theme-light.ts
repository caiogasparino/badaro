import { COLOR_LIGHT, COLOR_LIGHT_ORANGE } from '../colors/colorLight.colors'
import { POPPINS_FONT } from '../fonts/inter.font'
import { METRICS } from '../metrics/metrics'

export const THEME_LIGHT = {
  COLORS: COLOR_LIGHT,
  METRICS,
  FONTS: {
    PRIMARY: POPPINS_FONT,
  },
}
export const THEME_LIGHT_ORANGE = {
  COLORS: COLOR_LIGHT_ORANGE,
  METRICS,
  FONTS: {
    PRIMARY: POPPINS_FONT,
  },
}
